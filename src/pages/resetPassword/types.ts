export type EmailValue = {
  email: string;
};
export type EvToken = {
  ev: string | null;
};
export type FormValues = {
  pass: string;
  confirmPass: string;
};
export type NewPassword = {
  fp: string | null;
  newpw: string;
};
export type LocationExpiredState = {
  expiredUrl: boolean;
};
